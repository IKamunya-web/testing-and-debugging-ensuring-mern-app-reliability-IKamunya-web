// Server test setup
// Example: increase timeout for starting in-memory mongo or long-running ops
jest.setTimeout(20000);

// Additional global setup for server tests can go here (env vars, mocks, etc.)
