#!/usr/bin/env node
// Minimal script to show how to setup a test DB. Integration tests use mongodb-memory-server
// so this script will only print instructions for now.

console.log('Setup test DB: This project uses mongodb-memory-server for integration tests.');
console.log('No action required; tests will create an in-memory MongoDB instance at runtime.');
